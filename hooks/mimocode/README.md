# MiniMax Code Hooks

This TypeScript plugin uses MiniMax Code's OpenCode-compatible
`tool.execute.before` API and delegates command rewriting to `rtk rewrite`.

Install globally with `rtk init -g --mimocode`; the plugin is written to
`~/.config/mimocode/plugins/rtk.ts`.
